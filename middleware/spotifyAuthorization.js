// Check if spotify access token is attached as cookie
    // If so, Pass on original request
// Else
    // Request new access token from Spotify API
    // Attach as cookie to original request and pass on