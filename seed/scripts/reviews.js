// Imports
const { faker } = require('@faker-js/faker');

// Generate fake review data
const generateReviews = async () => {
    let reviews = [];

    for (let i = 0; i < 20; i++) {
        reviews.push({
            // TODO populate with review data based on model
        })
    }
};

// Exports
module.exports = {
    generateReviews
};